// data.mock.js — archived demo removed per project requirement.
// For compliance this file no longer contains demo data. All runtime inputs must come
// from `parametres.html` and `terrain.html` (or saved localStorage/IndexedDB state).

// If you previously used this for testing, restore from version control or an external backup.

/* intentionally empty */
